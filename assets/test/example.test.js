// Example test proving the vitest harness works.
// Replace with real tests for code in assets/js/.
import { describe, expect, it } from "vitest"

describe("vitest harness", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2)
  })
})
