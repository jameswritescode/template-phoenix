# Whitelist for `wt step copy-ignored` (run by the pre-start hook): gitignored
# files copied from the primary worktree so new worktrees start warm.
# deps + compiled BEAM output (includes dialyzer project PLTs and the
# esbuild/tailwind binaries) + JS packages + personal env. The pre-start
# hook re-derives the four managed .env keys (PORT, SUBDOMAIN, DB_PARTITION,
# MIX_TEST_PARTITION) per branch; other .env lines are carried over as-is.
deps/
_build/
assets/node_modules/
.env
